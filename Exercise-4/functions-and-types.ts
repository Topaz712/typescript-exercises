//Write a function greet that takes in a name (string) and returns a greeting (string).

function greet(name: string) {
  return `Hello, ${name}`;
}

greet("Topacio");//Hello, Topacio

/* can also be set like so:

const greeting = greet("Topacio");
console.log(greeting);//Hello, Topacio
*/
